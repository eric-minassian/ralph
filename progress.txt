# Ralph Progress Log
Started: 2026-03-30T23:20:55Z
---

## Iteration — 2026-03-30T23:33:00Z
Task: TASK-001 — Project Scaffolding & Core Infrastructure
Result: done
Summary: Initialized Vite + React 18 + TypeScript project with strict tsconfig, TanStack Router (file-based routing with Vite plugin), TanStack Query, Cloudscape Design System, MSW v2, ESLint with strict-type-checked config banning any/as/!, and Vitest. All three verification commands pass.
Learnings: @eslint/js must be explicitly installed as a devDependency — it's not bundled with eslint 9. Pin @eslint/js to ^9.x to match eslint ^9.x.
---

## Iteration — 2026-03-30T16:42:00Z
Task: TASK-002 — Testing Infrastructure
Result: done
Summary: Created test utilities (createTestQueryClient, createMockPermissions, renderWithProviders) with barrel export, Playwright config with dev server webServer setup, e2e/pages/ directory structure, and updated smoke test to validate the rendering setup.
Learnings: renderWithProviders should use a simple wrapper pattern (QueryClientProvider wrapping children) rather than embedding RouterProvider — RouterProvider takes over rendering and doesn't pass through children. Router can be added per-test when needed.
---

## Iteration — 2026-03-30T18:15:00Z
Task: TASK-003 — Internationalization Setup
Result: done
Summary: Configured react-i18next with 19 namespaced translation files (common, navigation, errors, validation, and 15 empty feature namespaces). Set up localStorage language persistence, Cloudscape I18nProvider integration in main.tsx, i18n test initialization in test-setup.ts, I18nextProvider wrapping in renderWithProviders, and unit tests validating translation resolution and fallback behavior.
Learnings: none
---

## Iteration — 2026-03-30T16:56:00Z
Task: TASK-004 — API Client & MSW Foundation
Result: done
Summary: Created CognitoIdentityProviderClient in src/api/client.ts (reads region/endpoint from env vars), query key factory in src/api/queryKeys.ts (all 15 domains with consistent ['cognito', domain, operation, ...params] structure), MSW Cognito handler dispatcher in src/mocks/handlers/cognitoHandler.ts (matches X-Amz-Target header, maps to operation resolvers, translates StoreError to AWS error format), BaseStore generic CRUD class in src/mocks/stores/baseStore.ts with pagination support, and factories directory. 37 tests passing.
Learnings: OperationResolver return type must be `unknown` not `unknown | Promise<unknown>` — the `no-redundant-type-constituents` rule flags `unknown` absorbing other union members. Use `Promise.resolve()` wrapper in the caller to handle both sync/async resolvers.
---

## Iteration — 2026-03-30T19:30:00Z
Task: TASK-005 — Permission System
Result: done
Summary: Created CognitoPermission union type (all 122 operations) in src/types/permissions.ts, PermissionProvider context (split into permissionContext.ts + PermissionProvider.tsx for react-refresh compatibility), usePermissions hook, PermissionGate component (single/anyOf/allOf logic), not-authorized route page, updated createMockPermissions to use real types with ALL_COGNITO_PERMISSIONS default, updated renderWithProviders with PermissionProvider wrapping. 54 tests passing.
Learnings: react-refresh/only-export-components warns when a file exports both a React context object and components — split context creation (plain .ts) from provider component (.tsx) to avoid the warning.
---

## Iteration — 2026-03-30T22:17:00Z
Task: TASK-006 — Layout & Navigation Shell
Result: done
Summary: Created Cloudscape AppLayout shell with data-driven SideNavigation (permission-gated via navigation config), TopNavigation with user menu, auto-generated BreadcrumbGroup from TanStack Router matches, Flashbar-based notification system (NotificationProvider context with add/dismiss), and renderWithRouter test utility for components requiring router context. 69 tests passing.
Learnings: TanStack Router's RouterProvider requires `await router.load()` before `render()` in tests — without it the route components don't mount synchronously. Created a `renderWithRouter` test utility that handles this. Cloudscape TopNavigation duplicates text in multiple DOM nodes (visible + aria), so tests must use `getAllByText` instead of `getByText`.
---

## Iteration — 2026-03-30T17:40:00Z
Task: TASK-007 — User Pool Hooks, MSW Handlers & List Page
Result: done
Summary: Created TanStack Query hooks for all 8 user pool operations (list, describe, create, update, delete, addCustomAttributes, getMfaConfig, setMfaConfig), MSW handlers with userPoolStore (CRUD + MFA config + pagination), mock data factory, Cloudscape Table-based list page with TextFilter/Pagination/CollectionPreferences/sorting, and comprehensive tests (store: 21, handlers: 13, hooks+list: 7). Total: 110 tests passing.
Learnings: AWS SDK needs dummy credentials (AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY) set in vitest.config.ts env for MSW-intercepted requests to work — without them the SDK's signing middleware fails before the request reaches MSW. Also, Cognito JSON wire format uses epoch seconds for dates, not ISO strings — added serializeDates() helper in cognitoHandler.ts to convert Date objects to epoch seconds in MSW responses. The `assertionStyle: 'never'` ESLint rule requires type guard functions (isRecord, isString, etc.) instead of `as` assertions for extracting values from Record<string, unknown>.
---

## Iteration — 2026-03-30T18:00:00Z
Task: TASK-008 — User Pool Create Wizard
Result: done
Summary: Built 5-step Cloudscape Wizard for user pool creation (pool settings, sign-in config, security/MFA, delivery config, review). Step components in src/components/UserPools/CreateWizard/ with typed form state, per-step validation, review with edit navigation, and MSW integration. Route at /user-pools/create. Updated list page create buttons to navigate to create route. 29 new tests (19 validation unit + 10 wizard integration). Total: 139 tests passing.
Learnings: Cloudscape Wizard duplicates step titles in multiple DOM nodes (nav sidebar, step header, content header) — use getAllByText in tests. Cloudscape FormField errorText may not render as plain text in jsdom — test validation by verifying blocked navigation instead. Controlled number inputs with Cloudscape Input require tripleClick+type pattern instead of clear+type since the controlled component re-renders from state after clear.
---
